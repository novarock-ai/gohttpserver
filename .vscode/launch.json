{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launching Web",
      "type": "go",
      "mode": "debug",
      "console": "integratedTerminal",
      "args": [
        "-r",
        "./var-ding-test", "--port", "80", "--pin-root", "--not-exist-auto-mkdir"],
      "cwd": "${workspaceFolder}",
      "request": "launch",
      "program": "${workspaceRoot}/.",
    },
  ]
}
