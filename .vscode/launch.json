{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launching Web",
      "type": "go",
      "mode": "debug",
      "console": "integratedTerminal",
      "args": [
        "-r",
        "./var", "--port", "9999", "--pin-root", "--upload"],
      "cwd": "${workspaceFolder}",
      "request": "launch",
      "program": "${workspaceRoot}/.",
    },
  ]
}
