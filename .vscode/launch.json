{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch",
      "type": "go",
      "mode": "debug",
      "console": "integratedTerminal",
      "args": [
        "-r",
        "./var-ding-test", "--port", "9999", "--pin-root"],
      "cwd": "${workspaceFolder}",
      "request": "launch",
      "program": "${workspaceRoot}/.",
    },
  ]
}
